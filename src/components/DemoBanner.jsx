import "./DemoBanner.css";

export default function DemoBanner() {
    return (
        <div className="demo-banner">
            ⚠️ Demo Mode — Data will not be saved
        </div>
    );
}
